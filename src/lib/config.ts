/**
 * Configuration centrale du site — change ces valeurs pour personnaliser
 * le nom et le logo affichés partout dans l'application, sans toucher au
 * reste du code. Voir docs/CUSTOMIZATION.md pour le guide pas-à-pas.
 */

export const SITE_NAME = "CoachOS";
export const SITE_TAGLINE = "Plateforme de gestion pour coachs sportifs.";

/**
 * Chemin vers ton logo, une fois déposé dans le dossier public/
 * (ex: "/logo.svg"). Laisse null pour afficher le nom du site en texte
 * avec le symbole par défaut à la place d'une image.
 */
export const LOGO_IMAGE_PATH: string | null = null;
